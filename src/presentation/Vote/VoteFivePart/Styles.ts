import styled from 'styled-components';

const innerWidth = 670;

export const VoteFivePartWrapper = styled.div`
  width: ${innerWidth}px;
  margin: 0 auto;
  padding-top: 36px;
  padding-bottom: 30px;
`